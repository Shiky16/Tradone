{
  "name": "tradone-web",
  "version": "1.0.0",
  "description": "Tradone relay — stateless multi-tenant proxy for the local-first web build (see server.js). User data lives client-side; this holds no per-user database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node dev-server.js"
  },
  "dependencies": {
    "avanza-api": "^2.1.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.1",
    "qrcode": "^1.5.4"
  }
}
